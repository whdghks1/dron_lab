{
  "name": "drone-lab-seoul-flight",
  "private": true,
  "version": "0.11.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "data:chunks": "node scripts/build-area-chunks.mjs",
    "data:chunks:cheonggyecheon": "node scripts/build-area-chunks.mjs cheonggyecheon",
    "perf:scene": "node --import tsx scripts/measure-world.ts",
    "test": "node --import tsx --test 'src/**/*.test.ts'",
    "test:watch": "node --import tsx --test --watch 'src/**/*.test.ts'"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "tsx": "^4.20.0",
    "vite": "^6.4.1"
  }
}
